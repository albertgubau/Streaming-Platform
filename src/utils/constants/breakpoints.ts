/**
 * Breakpoints for the app (Mobile first approach):
 * - `sm`: 0px - 767px (mobile)
 * - `md`: 768px - 1023px (tablet)
 * - `lg`: from 1024px (desktop)
 */
export enum MQ {
    sm = '390',
    md = '768',
    lg = '1024'
}
